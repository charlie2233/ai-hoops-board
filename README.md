# AI 战术板 · Step 1（MVP/PWA）

本包包含最小可用版本：半场/全场画布、攻防棋子拖拽、跑位虚线/传球箭头、撤销/重做、导出 PNG（带水印），以及首发战术/训练数据文件夹。可直接本地打开或部署到任意静态托管。

## 快速开始
1. 本地打开 `index.html`（推荐用 VSCode 的 Live Server / 任意 http server）。
2. 手机/iPad 访问后 “添加到主屏幕”，即可离线使用（视频播放需联网）。
3. 工具栏选择 **拖拽/跑位线/传球箭头**，绘制后可 **撤销/重做**；右下角 **导出 PNG**。

## 创建团队
- 由 [Atrak Student Tech Team](https://atrak.dev) 创建。

## 目录结构
```
/app (合并到根，见本包文件)
/plays/plays.json    # 21 张战术（可扩充）
/drills/drills.json  # 训练元数据（真实 YouTube drill 视频）
/assets/icon-*.png   # PWA 图标
index.html
main.js
manifest.json
service-worker.js
README.md
```

## 主题与样式
- 内置 **浅色 / 深色** 双主题，支持顶部一键切换并自动本地记忆（键：`uiThemeV1`）。
- 内置 **经典 / 动感** 样式变体（Light/Dark 均可叠加），自动本地记忆（键：`uiThemeStyleV1`）。
- 内置 **中文 / English** 语言切换，支持全站同步并本地记忆（键：`uiLangV1`）。
- 页面采用统一变量主题系统（`index` / `library` / `drills` 同步）。

## 已实现
- 半场/全场切换、棋子拖拽
- 跑位虚线、传球箭头（带箭头头部）
- 撤销/重做（针对线条对象）
- PNG 导出（右下角水印时间戳）
- PWA：离线缓存静态资源
- 轻 AI 提示条（基于本地规则的站位/线路建议）
- 顶部持球人选择（仅进攻 1-5）
- 全站 Light/Dark 主题切换（本地持久化）
- 全站 Classic/Vivid 样式切换（本地持久化）
- 双指缩放与平移（含视图重置）
- 沉浸模式（浏览器内）：一键隐藏顶部栏，左右双侧等宽控制栏，球场高度拉满屏幕
- 战术库可视化预览（半场/全场迷你画布）
- 战术库收藏/最近使用（筛选 + 排序）
- 画板顶部快速战术选择（收藏/最近/全部）+ 随机战术
- 训练页内嵌播放器（YouTube iframe）+ 一键应用关联战术
- 训练库扩展为 32 条真实 drill 视频（按战术 playId 关联）

## 待办（下一步）
- 回放导出为 GIF/视频（可选）
- 云端同步（账号可选）
- 多人协作房间（教练端/球员端）
- 自定义战术编辑并保存到本地 Playbook

—— 2025-09-20
